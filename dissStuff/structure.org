#+title: Structure
* DAG
{ hash :
        { index : #location/index of node
                {
                "links": [ [ hash, location ] ]#preceding nodes
                "mask": #how to apply the raw substrings
                "raw": [ [ substring, hash, length ] ]# parts
                "round": #round it was found
                }
        }
}
* Patterns
{ hash :
        { subhash : # sum of parts (same as hash if one part)
                {
                "f": #frequency of usages
                "score": #importance
                "round": #what itteration it came from
                "raw": [ [ substring, hash, length ] ]# parts
                }

        }
}

* DAG
** DAG=initialiseDAG(sentence)
create a DAG from begining to end of a sentence where each character is a node
** expandDAG(DAG,hashes)
recursively loop through DAG by mask

** Database and idf clause
** idf weighted jaccard:w

* Session
** QGraph
*** find review candidates
- sentences with activations and neighbour activations under a threshold
*** add new candidates
- sentences with generally comfortable cards and 1-2 new patterns
*** populate
- find minimum stable paths to new and reviews that include all involved known patterns for context
*** next
- figure out which card to show next depending on session state.
*** update node
- give a right or wrong for a node, if wrong add to see later and
